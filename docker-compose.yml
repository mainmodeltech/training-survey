# ─────────────────────────────────────────────────────────
#  RÉSEAUX
#  - dokploy-network : réseau externe partagé avec Traefik
#  - coraf-internal  : réseau privé entre les services
#    (la DB n'est PAS exposée à Traefik ni à l'extérieur)
# ─────────────────────────────────────────────────────────
networks:
  dokploy-network:
    external: true
  coraf-internal:
    driver: bridge
    internal: true

# ─────────────────────────────────────────────────────────
#  VOLUMES PERSISTANTS
# ─────────────────────────────────────────────────────────
volumes:
  coraf_pg_data:
    driver: local

# ─────────────────────────────────────────────────────────
#  SERVICES
# ─────────────────────────────────────────────────────────
services:

  # ── 1. PostgreSQL ──────────────────────────────────────
  coraf-db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - coraf_pg_data:/var/lib/postgresql/data
    networks:
      - coraf-internal          # DB isolée — jamais sur dokploy-network
    labels:
      - "traefik.enable=false"  # blindage : Traefik ne touche pas à Postgres
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s

  # ── 2. Backend FastAPI ──────────────────────────────────
  coraf-api:
    build:
      context: ./backend
    restart: unless-stopped
    depends_on:
      coraf-db:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@coraf-db:5432/${POSTGRES_DB}
      SECRET_KEY: ${SECRET_KEY}
      TOKEN_EXPIRE_MINUTES: ${TOKEN_EXPIRE_MINUTES:-480}
    networks:
      - coraf-internal          # parle à la DB
      - dokploy-network         # exposé via Traefik
    labels:
      - "traefik.enable=true"   # CORRIGÉ : était false, l'API ne répondait pas
      - "traefik.docker.network=dokploy-network"
      - "traefik.http.routers.coraf-api.rule=Host(`${DOMAIN}`) && (PathPrefix(`/api`) || PathPrefix(`/docs`) || PathPrefix(`/openapi.json`))"
      - "traefik.http.routers.coraf-api.entrypoints=websecure"
      - "traefik.http.routers.coraf-api.tls.certresolver=letsencrypt"
      - "traefik.http.routers.coraf-api.priority=10"
      - "traefik.http.services.coraf-api.loadbalancer.server.port=8000"

  # ── 3. Frontend React (Nginx) ───────────────────────────
  coraf-frontend:
    build:
      context: ./frontend
      args:
        VITE_API_URL: https://${DOMAIN}
    restart: unless-stopped
    depends_on:
      - coraf-api
    networks:
      - dokploy-network
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=dokploy-network"
      - "traefik.http.routers.coraf-frontend.rule=Host(`${DOMAIN}`)"
      - "traefik.http.routers.coraf-frontend.entrypoints=websecure"
      - "traefik.http.routers.coraf-frontend.tls.certresolver=letsencrypt"
      - "traefik.http.routers.coraf-frontend.priority=1"
      - "traefik.http.services.coraf-frontend.loadbalancer.server.port=80"

  # ── 4. Adminer ─────────────────────────────────────────
  coraf-adminer:
    image: adminer:latest
    restart: unless-stopped
    depends_on:
      - coraf-db
    networks:
      - coraf-internal          # parle à coraf-db via réseau privé
      - dokploy-network         # exposé via Traefik
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=dokploy-network"
      - "traefik.http.routers.coraf-adminer.rule=Host(`adminer-coraf.model-technologie.com`)"
      - "traefik.http.routers.coraf-adminer.entrypoints=websecure"
      - "traefik.http.routers.coraf-adminer.tls.certresolver=letsencrypt"
      - "traefik.http.services.coraf-adminer.loadbalancer.server.port=8080"
